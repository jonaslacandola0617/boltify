<x-admin-layout><div><p class="text-sm font-medium text-orange-600">Customer directory</p><h1 class="text-3xl font-semibold">Customers</h1><p class="text-sm text-zinc-500">Registered accounts and purchasing activity.</p></div><form method="get" class="flex gap-3 rounded-2xl border bg-white p-4"><input name="search" value="{{ $search }}" placeholder="Search name or email" class="min-w-0 flex-1 rounded-xl border-zinc-200 text-sm"><button class="rounded-xl bg-zinc-900 px-4 py-2 text-sm font-semibold text-white">Search</button></form><section class="overflow-hidden rounded-2xl border bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[700px] text-left text-sm"><thead class="bg-zinc-50 text-xs uppercase text-zinc-400"><tr><th class="px-5 py-3">Customer</th><th>Role</th><th>Orders</th><th>Lifetime value</th><th>Joined</th></tr></thead><tbody class="divide-y">@forelse($users as $user)<tr><td class="px-5 py-4"><p class="font-medium">{{ $user->name }}</p><p class="text-xs text-zinc-500">{{ $user->email }}</p></td><td><span class="rounded-full bg-zinc-100 px-2 py-1 text-xs">{{ $user->is_admin?'Admin':'Customer' }}</span></td><td>{{ $user->orders_count }}</td><td class="font-semibold">₱{{ number_format(($user->lifetime_value??0)/100,2) }}</td><td class="text-zinc-500">{{ $user->created_at->format('M d, Y') }}</td></tr>@empty<tr><td colspan="5" class="p-12 text-center text-zinc-400">No customers found.</td></tr>@endforelse</tbody></table></div>@if($users->hasPages())<div class="border-t px-5 py-4">{{ $users->links() }}</div>@endif</section></x-admin-layout>
