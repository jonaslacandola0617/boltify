<x-admin-layout><div><a href="{{ route('admin.product.index') }}" class="text-sm font-medium text-orange-600">← Back to products</a><h1 class="mt-2 text-3xl font-semibold">Edit product</h1><p class="text-sm text-zinc-500">Update catalog details and inventory.</p></div><form action="{{ route('admin.product.update',$product) }}" method="post" enctype="multipart/form-data" class="grid gap-6 xl:grid-cols-[1fr_360px]">@csrf @method('PUT')<section class="grid gap-5 rounded-2xl border bg-white p-6 sm:grid-cols-2"><label class="sm:col-span-2"><span class="mb-1 block text-sm font-medium">Name</span><input name="name" value="{{ old('name',$product->name) }}" required class="w-full rounded-xl border-zinc-200"></label><label><span class="mb-1 block text-sm font-medium">Price</span><input type="number" step="0.01" min="0" name="price" value="{{ old('price',$product->price) }}" required class="w-full rounded-xl border-zinc-200"></label><label><span class="mb-1 block text-sm font-medium">Stock</span><input type="number" min="0" name="stock" value="{{ old('stock',$product->stock) }}" required class="w-full rounded-xl border-zinc-200"></label><label class="sm:col-span-2"><span class="mb-1 block text-sm font-medium">Category</span><select name="category" class="w-full rounded-xl border-zinc-200">@foreach($categories as $c)<option value="{{ $c->id }}" @selected(old('category',$product->categoryId)===$c->id)>{{ $c->name }}</option>@endforeach</select></label><label class="sm:col-span-2"><span class="mb-1 block text-sm font-medium">Description</span><textarea name="description" rows="8" required class="w-full rounded-xl border-zinc-200">{{ old('description',$product->description) }}</textarea></label></section><aside class="space-y-4"><div class="rounded-2xl border bg-white p-5"><h2 class="font-semibold">Images</h2><p class="text-xs text-zinc-500">New uploads replace the current set.</p>@php($imgs=json_decode($product->images,true)?:[])<div class="mt-4 grid grid-cols-3 gap-2">@foreach($imgs as $img)<img src="{{ asset('storage/'.$img) }}" class="aspect-square rounded-xl object-cover">@endforeach</div><input type="file" name="images[]" multiple accept=".jpg,.jpeg,.png,.webp" class="mt-4 w-full text-xs"></div><button class="w-full rounded-xl bg-orange-500 px-4 py-3 font-semibold text-white">Save changes</button></aside></form></x-admin-layout>
