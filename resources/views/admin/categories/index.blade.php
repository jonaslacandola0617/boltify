<x-admin-layout><div class="flex flex-col gap-6"><div><p class="text-sm font-medium text-orange-600">Catalog structure</p><h1 class="text-3xl font-semibold">Categories</h1><p class="text-sm text-zinc-500">Organize storefront filters without editing the database.</p></div><div class="grid gap-6 lg:grid-cols-[1fr_340px]"><section class="overflow-hidden rounded-2xl border bg-white shadow-sm"><div class="border-b px-5 py-4"><h2 class="font-semibold">Store categories</h2></div><div class="divide-y">@forelse($categories as $category)<div class="flex items-center justify-between px-5 py-4"><div><p class="font-medium">{{ $category->name }}</p><p class="text-xs text-zinc-500">{{ $category->products_count }} product(s)</p></div>@if($category->products_count===0)<form method="post" action="{{ route('admin.categories.delete',$category) }}">@csrf @method('DELETE')<button class="rounded-lg border px-3 py-2 text-xs font-medium text-red-600">Delete</button></form>@else<span class="rounded-full bg-zinc-100 px-2 py-1 text-xs text-zinc-500">In use</span>@endif</div>@empty<p class="p-8 text-center text-sm text-zinc-400">No categories.</p>@endforelse</div></section><aside class="h-fit rounded-2xl border bg-white p-5 shadow-sm"><h2 class="font-semibold">Add category</h2><form method="post" action="{{ route('admin.categories.store') }}" class="mt-4 space-y-3">@csrf<input name="name" value="{{ old('name') }}" required maxlength="100" placeholder="e.g. Power Tools" class="w-full rounded-xl border-zinc-200"><button class="w-full rounded-xl bg-orange-500 px-4 py-2.5 text-sm font-semibold text-white">Create category</button></form></aside></div></div></x-admin-layout>
