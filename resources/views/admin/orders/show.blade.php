<x-admin-layout><div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><a href="{{ route('admin.orders.index') }}" class="text-sm font-medium text-orange-600">← Back to orders</a><h1 class="mt-2 text-3xl font-semibold">Order #{{ str($order->id)->substr(0,8) }}</h1><p class="text-sm text-zinc-500">{{ $order->created_at->format('M d, Y h:i A') }}</p></div><span class="self-start rounded-full bg-zinc-100 px-3 py-1 text-sm font-semibold">{{ ucfirst($order->status) }}</span></div><div class="grid gap-6 xl:grid-cols-[1fr_360px]"><section class="overflow-hidden rounded-2xl border bg-white"><div class="border-b px-5 py-4 font-semibold">Items</div><div class="divide-y">@foreach($order->products as $product)@php($imgs=json_decode($product->images,true)?:[])@php($unitPrice=$product->pivot->unit_price??$product->price)<div class="flex items-center gap-4 px-5 py-4">@if($imgs)<img src="{{ asset('storage/'.$imgs[0]) }}" class="h-14 w-14 rounded-xl object-cover">@endif<div class="min-w-0 flex-1"><p class="truncate font-medium">{{ $product->name }}</p><p class="text-xs text-zinc-500">Qty {{ $product->pivot->quantity }}</p></div><strong>₱{{ number_format($unitPrice*$product->pivot->quantity,2) }}</strong></div>@endforeach</div><div class="flex justify-between border-t bg-zinc-50 px-5 py-4"><span>Total charged</span><strong class="text-xl">₱{{ number_format($order->total/100,2) }}</strong></div></section><aside class="space-y-5"><section class="rounded-2xl border bg-white p-5"><h2 class="font-semibold">Customer</h2><div class="mt-3 space-y-2 text-sm"><p><strong>{{ $order->name }}</strong></p><p>{{ $order->email }}</p><p class="text-zinc-600">{{ $order->address }}, {{ $order->city }}, {{ $order->country }}</p></div></section><section class="rounded-2xl border bg-white p-5"><h2 class="font-semibold">Payment</h2><p class="mt-3 break-all text-xs text-zinc-500">{{ $order->payment_intent }}</p><p class="mt-2 text-sm capitalize">{{ $order->payment_method }} · {{ $order->payment_status }}</p></section>@if($order->status==='complete'&&!$order->refund)<form method="post" action="{{ route('admin.orders.refund',$order) }}" class="rounded-2xl border border-red-200 bg-red-50 p-5">@csrf<input type="hidden" name="reason" value="requested_by_admin"><h2 class="font-semibold text-red-900">Refund order</h2><p class="mt-1 text-xs text-red-700">Refund through Stripe and restore stock when successful.</p><button class="mt-4 w-full rounded-xl bg-red-600 px-4 py-2.5 text-sm font-semibold text-white">Refund through Stripe</button></form>@endif</aside></div></x-admin-layout>
