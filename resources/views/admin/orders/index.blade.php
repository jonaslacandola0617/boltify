<x-admin-layout><div><p class="text-sm font-medium text-orange-600">Sales operations</p><h1 class="text-3xl font-semibold">Orders</h1><p class="text-sm text-zinc-500">Review transactions and refunds.</p></div><form method="get" class="grid gap-3 rounded-2xl border bg-white p-4 md:grid-cols-[1fr_200px_auto]"><input name="search" value="{{ $search }}" placeholder="Order, customer, email, payment intent" class="rounded-xl border-zinc-200 text-sm"><select name="status" class="rounded-xl border-zinc-200 text-sm"><option value="">All statuses</option><option value="complete" @selected($status==='complete')>Complete</option><option value="refunded" @selected($status==='refunded')>Refunded</option><option value="pending" @selected($status==='pending')>Pending</option></select><button class="rounded-xl bg-zinc-900 px-4 py-2 text-sm font-semibold text-white">Filter</button></form><section class="overflow-hidden rounded-2xl border bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[850px] text-left text-sm"><thead class="bg-zinc-50 text-xs uppercase text-zinc-400"><tr><th class="px-5 py-3">Order</th><th>Customer</th><th>Status</th><th>Total</th><th>Date</th><th></th></tr></thead><tbody class="divide-y">@forelse($orders as $order)<tr><td class="px-5 py-4 font-medium">#{{ str($order->id)->substr(0,8) }}</td><td><p class="font-medium">{{ $order->name }}</p><p class="text-xs text-zinc-500">{{ $order->email }}</p></td><td><span class="rounded-full bg-zinc-100 px-2 py-1 text-xs font-semibold">{{ ucfirst($order->status) }}</span></td><td class="font-semibold">₱{{ number_format($order->total/100,2) }}</td><td class="text-zinc-500">{{ $order->created_at->format('M d, Y') }}</td><td class="px-5 text-right"><a href="{{ route('admin.orders.show',$order) }}" class="rounded-lg border px-3 py-2 text-xs font-semibold">Details</a></td></tr>@empty<tr><td colspan="6" class="p-12 text-center text-zinc-400">No orders found.</td></tr>@endforelse</tbody></table></div>@if($orders->hasPages())<div class="border-t px-5 py-4">{{ $orders->links() }}</div>@endif</section></x-admin-layout>
