<x-app-layout><section class="overflow-hidden rounded-3xl bg-gradient-to-br from-amber-300 via-orange-400 to-orange-600 p-6 sm:p-10"><div class="max-w-2xl"><span class="rounded-full bg-white/80 px-3 py-1 text-xs font-semibold uppercase tracking-wider text-orange-700">Built for the job</span><h1 class="mt-4 text-3xl font-bold tracking-tight text-zinc-950 sm:text-5xl">Tools and supplies for projects that need to get done.</h1><p class="mt-3 max-w-xl text-sm leading-relaxed text-orange-950/70 sm:text-base">Browse hardware essentials, safety gear, construction materials, and more from Boltify.</p></div></section><div class="grid gap-6 lg:grid-cols-[260px_1fr]"><aside><x-filter :categories="$categories" :filter="$filter" :minPrice="$minPrice" :maxPrice="$maxPrice"/></aside><section class="min-w-0"><div class="mb-5 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 class="text-xl font-semibold">Products</h2><p class="text-sm text-zinc-500">{{ $products->total() }} item(s){{ $search ? ' matching “'.$search.'”':'' }}</p></div><form method="get" class="flex items-center gap-2">@foreach(request()->except('sort','page') as $key=>$value)@if(is_array($value))@foreach($value as $v)<input type="hidden" name="{{ $key }}[]" value="{{ $v }}">@endforeach @else<input type="hidden" name="{{ $key }}" value="{{ $value }}">@endif @endforeach<select name="sort" onchange="this.form.submit()" class="rounded-xl border-zinc-200 bg-white text-sm"><option value="newest" @selected($sort==='newest')>Newest</option><option value="price_low" @selected($sort==='price_low')>Price: low to high</option><option value="price_high" @selected($sort==='price_high')>Price: high to low</option><option value="name" @selected($sort==='name')>Name</option></select></form></div><div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4">@forelse($products as $product)<livewire:product-card :product="$product" :key="$product->id"/>@empty<div class="col-span-full rounded-2xl border border-dashed bg-white p-12 text-center text-sm text-zinc-400">No products match these filters.</div>@endforelse</div>@if($products->hasPages())<div class="mt-8">{{ $products->links() }}</div>@endif</section></div></x-app-layout>
