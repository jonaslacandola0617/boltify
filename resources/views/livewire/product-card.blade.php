@php($images=json_decode($product->images,true)?:[])<article class="group flex h-full flex-col overflow-hidden rounded-2xl border border-zinc-200 bg-white shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><a href="{{ route('product.show',$product) }}" class="block aspect-[4/3] overflow-hidden bg-zinc-100">@if($images)<img src="{{ asset('storage/'.$images[0]) }}" alt="{{ $product->name }}" class="h-full w-full object-cover transition duration-300 group-hover:scale-105">@endif</a><div class="flex flex-1 flex-col p-4"><p class="text-xs font-medium text-orange-600">{{ $product->category?->name }}</p><a href="{{ route('product.show',$product) }}" class="mt-1 line-clamp-2 font-semibold text-zinc-900">{{ $product->name }}</a><p class="mt-2 line-clamp-2 text-xs leading-relaxed text-zinc-500">{{ $product->description }}</p><div class="mt-auto flex items-end justify-between gap-3 pt-4"><div><p class="text-lg font-bold">₱{{ number_format($product->price,2) }}</p><p class="text-xs {{ $product->stock>0?'text-zinc-400':'text-red-500' }}">{{ $product->stock>0?$product->stock.' in stock':'Out of stock' }}</p></div><button wire:click="store" @disabled($product->stock<1) class="rounded-xl bg-orange-500 px-3 py-2 text-xs font-semibold text-white disabled:cursor-not-allowed disabled:bg-zinc-300">Add to cart</button></div></div></article>
