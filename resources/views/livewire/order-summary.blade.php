<aside class="h-fit rounded-2xl border bg-white p-5 shadow-sm lg:sticky lg:top-28"><h2 class="font-semibold">Order summary</h2><div class="mt-4 space-y-3">@foreach($summary as $item)<div class="flex justify-between gap-4 text-sm"><div><p class="font-medium">{{ $item['name'] }}</p><p class="text-xs text-zinc-500">{{ $item['quantity'] }} × ₱{{ number_format($item['price'],2) }}</p></div><span>₱{{ number_format($item['totalPrice'],2) }}</span></div>@endforeach</div><div class="my-4 border-t"></div><div class="flex items-center justify-between"><span class="text-sm text-zinc-500">Subtotal · {{ $count }} item(s)</span><strong class="text-xl">₱{{ number_format($subTotal,2) }}</strong></div><a href="{{ route('checkout.index') }}" class="mt-5 block rounded-xl bg-orange-500 px-4 py-3 text-center text-sm font-semibold text-white">Checkout</a></aside>
