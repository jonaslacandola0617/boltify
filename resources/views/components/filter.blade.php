@props(['categories','filter','minPrice','maxPrice'])<form action="{{ route('feed') }}" method="get" class="rounded-2xl border border-zinc-200 bg-white p-5 shadow-sm"><div class="flex items-center justify-between"><h2 class="font-semibold">Filters</h2><a href="{{ route('feed') }}" class="text-xs font-medium text-orange-600">Reset</a></div>@if(request('search'))<input type="hidden" name="search" value="{{ request('search') }}">@endif<div class="mt-5"><p class="mb-2 text-sm font-medium">Categories</p><div class="space-y-2">@foreach($categories as $category)<label class="flex items-center gap-2 text-sm text-zinc-600"><input type="checkbox" name="categories[]" value="{{ $category->id }}" @checked(in_array($category->id,$filter??[])) class="rounded border-zinc-300 text-orange-500 focus:ring-orange-400">{{ $category->name }}</label>@endforeach</div></div><div class="mt-5 grid grid-cols-2 gap-2"><label><span class="mb-1 block text-xs text-zinc-500">Min price</span><input type="number" name="min_price" min="0" value="{{ $minPrice }}" class="w-full rounded-lg border-zinc-200 text-sm"></label><label><span class="mb-1 block text-xs text-zinc-500">Max price</span><input type="number" name="max_price" min="0" value="{{ $maxPrice }}" class="w-full rounded-lg border-zinc-200 text-sm"></label></div><button class="mt-5 w-full rounded-xl bg-zinc-900 px-4 py-2.5 text-sm font-semibold text-white">Apply filters</button></form>
