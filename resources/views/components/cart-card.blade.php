@props(['product']) @php($imgs=json_decode($product->images,true)?:[])<article class="flex gap-4 rounded-2xl border bg-white p-4 shadow-sm">@if($imgs)<img src="{{ asset('storage/'.$imgs[0]) }}" alt="{{ $product->name }}" class="h-24 w-24 shrink-0 rounded-xl object-cover sm:h-28 sm:w-28">@endif<div class="min-w-0 flex-1"><div class="flex items-start justify-between gap-3"><div><h2 class="font-semibold">{{ $product->name }}</h2><p class="text-xs text-zinc-500">{{ $product->category?->name }}</p></div><form action="{{ route('cart.update',Auth::user()->cart) }}" method="post">@csrf @method('PUT')<input type="hidden" name="productId" value="{{ $product->id }}"><button class="rounded-lg p-2 text-zinc-400 hover:bg-red-50 hover:text-red-600"><i data-feather="x" class="h-4 w-4"></i></button></form></div><p class="mt-2 line-clamp-2 text-xs text-zinc-500">{{ $product->description }}</p><div class="mt-4 flex flex-wrap items-center justify-between gap-3"><strong>₱{{ number_format($product->price,2) }}</strong><livewire:quantity :quantity="$product->pivot->quantity" :product="$product->id" :key="'qty-'.$product->id"/></div></div></article>
