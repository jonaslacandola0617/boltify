<x-app-layout><div><p class="text-sm font-medium text-orange-600">Account</p><h1 class="text-3xl font-semibold">My orders</h1><p class="text-sm text-zinc-500">Track purchases and refund status.</p></div><section class="overflow-hidden rounded-2xl border bg-white shadow-sm"><div class="divide-y">@forelse($orders as $order)<a href="{{ route('order.show',$order) }}" class="grid gap-3 p-5 hover:bg-zinc-50 sm:grid-cols-[1fr_auto_auto] sm:items-center"><div><p class="font-semibold">Order #{{ str($order->id)->substr(0,8) }}</p><p class="text-xs text-zinc-500">{{ $order->created_at->format('M d, Y') }} · {{ $order->products_count }} product line(s)</p></div><span class="rounded-full bg-zinc-100 px-2 py-1 text-xs font-semibold">{{ ucfirst($order->status) }}</span><strong>₱{{ number_format($order->total/100,2) }}</strong></a>@empty<p class="p-12 text-center text-sm text-zinc-400">You have no orders yet.</p>@endforelse</div></section></x-app-layout>
