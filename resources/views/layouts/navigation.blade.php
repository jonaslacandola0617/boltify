<nav class="sticky top-0 z-50 border-b border-zinc-200 bg-white/95 backdrop-blur"><div class="mx-auto flex max-w-[1440px] items-center gap-4 px-4 py-3 sm:px-6 lg:px-8"><a href="{{ route('feed') }}" class="shrink-0"><x-application-logo class="text-2xl"/></a><form method="get" action="{{ route('feed') }}" class="hidden min-w-0 flex-1 sm:flex"><div class="flex w-full items-center overflow-hidden rounded-xl border border-zinc-200 bg-zinc-50 focus-within:border-orange-400"><input name="search" value="{{ request('search') }}" placeholder="Search tools and supplies" class="min-w-0 flex-1 border-0 bg-transparent px-4 py-2.5 text-sm focus:ring-0"><button class="bg-orange-500 px-4 py-2.5"><i data-feather="search" class="h-4 w-4 stroke-white"></i></button></div></form><div class="ml-auto flex items-center gap-2">@auth<a href="{{ route('order.index') }}" class="hidden rounded-lg px-3 py-2 text-sm font-medium hover:bg-zinc-100 md:block">Orders</a>@if(Auth::user()->cart)<a href="{{ route('cart.show',Auth::user()->cart) }}" class="relative rounded-lg p-2 hover:bg-zinc-100"><i data-feather="shopping-cart" class="h-5 w-5"></i><livewire:count/></a>@endif<div x-data="{open:false}" class="relative"><button @click="open=!open" class="rounded-lg border border-zinc-200 px-3 py-2 text-sm font-medium">{{ Str::limit(Auth::user()->name,16) }}</button><div x-cloak x-show="open" @click.outside="open=false" class="absolute right-0 mt-2 w-48 rounded-xl border bg-white p-2 shadow-lg"><a href="{{ route('profile.edit') }}" class="block rounded-lg px-3 py-2 text-sm hover:bg-zinc-50">Profile</a><a href="{{ route('order.index') }}" class="block rounded-lg px-3 py-2 text-sm hover:bg-zinc-50">My orders</a>@if(Auth::user()->is_admin)<a href="{{ route('admin.overview') }}" class="block rounded-lg px-3 py-2 text-sm font-medium text-orange-600 hover:bg-orange-50">Admin dashboard</a>@endif<form method="post" action="{{ route('logout') }}">@csrf<button class="w-full rounded-lg px-3 py-2 text-left text-sm hover:bg-zinc-50">Logout</button></form></div></div>@else<a href="{{ route('login') }}" class="rounded-xl bg-orange-500 px-4 py-2.5 text-sm font-semibold text-white">Sign in</a>@endauth</div></div><form method="get" action="{{ route('feed') }}" class="mx-auto flex max-w-[1440px] px-4 pb-3 sm:hidden"><input name="search" value="{{ request('search') }}" placeholder="Search products" class="w-full rounded-xl border-zinc-200 text-sm"></form></nav>
