<x-app-layout><div><p class="text-sm font-medium text-orange-600">Secure checkout</p><h1 class="text-3xl font-semibold">Review and pay</h1><p class="text-sm text-zinc-500">Continue to Stripe for payment and shipping details.</p></div><div class="grid gap-6 lg:grid-cols-[1fr_380px]"><section class="rounded-2xl border bg-white p-5 shadow-sm"><h2 class="font-semibold">Order items</h2><div class="mt-4 divide-y">@foreach($summary as $item)<div class="flex justify-between gap-4 py-3"><div><p class="font-medium">{{ $item['name'] }}</p><p class="text-xs text-zinc-500">{{ $item['category'] }} · {{ $item['quantity'] }} × ₱{{ number_format($item['price'],2) }}</p></div><strong>₱{{ number_format($item['totalPrice'],2) }}</strong></div>@endforeach</div></section><aside class="h-fit rounded-2xl border bg-white p-5 shadow-sm"><h2 class="font-semibold">Payment</h2><form action="{{ route('checkout.store') }}" method="post" class="mt-4">@csrf<label class="flex items-center gap-3 rounded-xl border p-4"><input type="radio" name="payment_method" value="card" checked required class="text-orange-500 focus:ring-orange-400"><i data-feather="credit-card" class="h-5 w-5"></i><span class="text-sm font-medium">Credit / debit card</span></label><div class="mt-5 space-y-2 text-sm"><div class="flex justify-between"><span class="text-zinc-500">Subtotal</span><span>₱{{ number_format($subTotal,2) }}</span></div><div class="flex justify-between"><span class="text-zinc-500">Shipping</span><span>₱58.00</span></div><div class="flex justify-between border-t pt-3 text-lg font-semibold"><span>Total</span><span>₱{{ number_format($subTotal+58,2) }}</span></div></div><button class="mt-5 w-full rounded-xl bg-orange-500 px-4 py-3 font-semibold text-white">Continue to Stripe</button></form></aside></div></x-app-layout>
